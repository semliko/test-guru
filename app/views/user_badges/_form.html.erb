<%= form_with(model: user_badge) do |form| %>
  <% if user_badge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_badge.errors.count, "error") %> prohibited this user_badge from being saved:</h2>

      <ul>
        <% user_badge.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :badge_id %>
    <%= form.text_field :badge_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
